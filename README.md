# Fulldome Viewer

Браузерное приложение для предпросмотра контента в формате fulldome / domemaster 180° (azimuthal equidistant fisheye) на 3D-модели планетарного купола.

## Запуск

```bash
python -m http.server 5173
```

Откройте http://localhost:5173 в браузере.

## Параметры купола

| Параметр | Значение |
|---|---|
| Диаметр | 29,4 м |
| Радиус по основанию | 14,7 м |
| Высота | 10 м |
| Наклон вперёд | 5° |

## Управление

- **ЛКМ + движение** — вращение камеры
- **Колесо мыши** — изменение FOV (30°–120°)
- **Клик по креслу** — перемещение к месту
- **Drag & drop** — загрузка изображения или видео на купол

## Поддерживаемые форматы

- Изображения: PNG, JPG, WebP
- Видео: MP4, WebM

## VR (WebXR)

Кнопка «ENTER VR» показывается только если браузер и устройство поддерживают WebXR (immersive-vr). **Почему может не быть поддержки:**

- **HTTPS обязателен** — на HTTP WebXR недоступен (кроме localhost)
- **Браузер** — поддерживают Chrome (Android/Desktop), Firefox, Edge; Safari (iOS) — ограниченно или нет
- **Устройство** — для полноценного VR нужен шлем или режим Cardboard; на многих телефонах без шлема режим «immersive-vr» не предлагается

Если кнопки нет — приложение просто не показывает её, вместо сообщения «VR NOT SUPPORTED».

## Публикация и S3

- **Опубликовать** — загрузить текущий контент на S3 (если локальный файл) и скопировать ссылку на сферу. Требует развёрнутого [presigner Worker](worker/README.md).
- **По URL** — вставить ссылку на фото/видео (например, из Yandex Object Storage).
- Настройка CORS и публичного доступа для Yandex S3: [docs/S3_SETUP.md](docs/S3_SETUP.md)
